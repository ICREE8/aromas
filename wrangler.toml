# Cloudflare Pages configuration
name = "aromas"
pages_build_output_dir = "."
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# AI binding enables Workers AI fallback for scanner
[ai]
binding = "AI"
